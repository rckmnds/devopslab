[tox]
envlist = py39
skipsdist = True
 
[testenv]
deps =
    pytest
    coverage
    pytest-cov
commands = pytest --cov=rckmnds/devopslab/ --cov-report=xml --cov-config=tox.ini --cov-branch
[coverage:run]

